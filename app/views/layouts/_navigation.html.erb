<nav class="navbar navbar-expand-lg navbar-light bg-light">
        <%= link_to "ALPHA BLOG" , root_path, class: "navbar-brand" , id: "logo"%>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
      
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
             <%= link_to "About", about_path, class: "nav-link" %> 
            </li>
            <li class="nav-item">
    
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Articles
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to "Create new article", new_article_path, class: "dropdown-item" %>
                <%= link_to "View articles", articles_path, class: "dropdown-item" %>
                <div class="dropdown-divider"></div>
              </div>
            </li>
            
            <li class="nav-item">
            <% if blogger_signed_in? %>
            <%= link_to 'sign_out' , destroy_blogger_session_path, class: "nav-link" ,method: :delete %>
            <%else%>

            <%=   link_to 'sign_in' ,  new_blogger_session_path, class: "nav-link", method: :post %>
            <%end%>
            <% if blogger_signed_in? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= "(Admin)" if current_blogger.admin? %> Profile [<%= current_blogger.email%>]
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              
          <%= link_to "View Profile", blogger_path(current_blogger), class: "dropdown-item" %>
          <%= link_to "Edit Profile", edit_blogger_registration_path, class: "dropdown-item" %> 
          <%= link_to "Create Article", new_article_path, class: "dropdown-item" %> 
          <%= link_to "Sign out", destroy_blogger_session_path, class: "dropdown-item", method: :delete%> 
          <%= link_to "Delete profile", blogger_path(current_blogger), class: "dropdown-item text-danger", method: :delete,data: {confirm: "Are you sure?"} %>

              </div>
            </li>
            <%end%>
            
            </li>
          </ul>
          <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>

          </form>
        </div>
      </nav>
